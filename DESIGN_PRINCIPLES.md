# AI News Digest Design Principles

这份文档是 AI News Digest 的设计 prompt 与视觉交付原则。它适用于默认 ImageGen、Artist Lottery、HTML 预览和飞书文档截图；它不是一个固定风格模板，而是一套让每次输出都更有判断力的方法。

方法来自「把 AI 变成 world-class designer」一文的核心启发：先发散，再定义身份，再用图像与批评推进，最后靠删减完成交付。参考文章：[How to turn your AI into a world-class designer](https://www.lennysnewsletter.com/p/how-to-turn-your-ai-into-a-world)。

## 0. Judgment Before Styling

- 设计先回答“今天真正变化的变量是什么”，再回答“它应该长什么样”。能力、成本、分发、供给、监管和组织采用必须与发布热闹区分。
- 每个视觉选择都要能解释趋势：主体为什么是它，空间关系为什么这样安排，材料和光为什么这样工作，读者先看什么、随后如何回查证据。
- 艺术家姓名、风格标签和参考图只提供 provenance、语境或 moodboard，不是复制指令。不能靠调色板、点阵、切口、笔触、logo 或神圣符号制造“像”。
- 用户的判断是最后的审美授权。模型可以提出选项和反例，不能把“模型说完成了”当作完成标准。

## 1. Discover：先发散，再深入

每次日报视觉任务先生成至少三个真正不同的方向。方向差异至少体现在以下三项中的两项：

- 构图或空间关系：观看距离、主体位置、空白、路径、阻断、层叠或裁切不同。
- 材料或光的行为：表面、背层、透明度、颗粒、阴影、反射、重量或时间状态不同。
- 情绪或编辑回应：冷静、压迫、诱惑、荒诞、亲密、庄严或不安等阅读反应不同。

候选方向不能只是同一张图换色、换标题或换一个艺术家名字。应先用具体的视觉野心和工作假设描述方向，再选择值得继续的方向。保留未采用的方向和 prompt，它们是未来模型、Lottery 和案例库的研究材料，不是废稿。

探索可使用一个只保留 fingerprint 的 design seed 为搜索注入变化。seed 不得渲染到图中，也不得覆盖趋势事实、艺术机制或文化安全边界。

## 2. Define：定义一个可执行的设计身份

选定方向后，写成短而具体的 design brief，至少回答：

1. **Intent**：这张图要让读者感到什么，并理解哪个判断？
2. **World**：这个视觉世界的工作想法是什么，而不只是“某某风格”？
3. **Subject**：主体、空间关系和构图骨架是什么？
4. **Material**：材料、光、表面动作和时间状态如何承载结构变量？
5. **Information**：标题、核心判断、辅助标签和证据入口的阅读顺序是什么？
6. **Invariants**：哪些内容不能被视觉实验改变？哪些元素明确禁止？
7. **Output intent**：它服务于飞书文档插图、公众号头图、HTML section 还是另一个阅读场景？

### Independent Critic

- critic 使用新上下文，只接收最终截图、最终趋势输入、目标审美/契约和必要参考基线；不接收生成器代码、私有推理、旧批评或生成器自评。
- critic 以顶级设计工作室标准指出最大的高层缺陷，再指出关键细节；意见必须具体、锋利、可执行，主动惩罚 AI 常见套路、无价值发光、泛化科技符号和表面风格模仿。
- 每轮评分 0-10。通常做 1-2 轮修复后检查是否收敛；独立评分未达到 9 分或仍有未解决的大问题时，不宣称完成。
- critic 的职责是判断，不是替生成器解释意图。最终截图如何呈现，比 prompt 里声称想做什么更重要。

## 3. Enrich：让 ImageGen 建立世界

ImageGen 是一等的设计工具，负责建立主体、空间、材料、光和状态的真实感与想象力；它不是给一张代码截图补一层漂亮背景。

所有图像 prompt 按以下顺序组织：

1. `scene_or_backdrop`：世界、场景或基底的观看条件。
2. `subject`：与趋势结构变量相关的主体。
3. `key_details`：材料动作、空间关系、光、尺度和必要的文字层级。
4. `constraints`：16:9、中文可读性、禁止元素、文化安全、不要复制参考图。
5. `output_intent`：这张图作为日报视觉入口要让读者看懂什么。

默认 ImageGen 与 Artist Lottery 必须遵守同一原则：先关系和动作，再材质、形态和颜色；先趋势命题，再视觉语法。三张趋势图各自生成，内容不变量保持一致，但主体、构图骨架和阅读方向必须重新设计。

### Artist Lottery 的专门映射

```text
馆藏/目录 provenance
  -> 机制提取
  -> 三个方向发散
  -> 方向定义与趋势转译
  -> 三张独立 ImageGen 图
  -> 新上下文 critic
  -> 删减审计与逐图 QA
  -> accepted / rework / rejected 案例
  -> 下一次 Lottery 的记忆
```

艺术家或传统只能提供经来源支持的空间关系、材料动作、节奏/留白和观看条件。`style_contract` 负责机制；`design_process` 负责方向探索、身份定义、ImageGen 丰富和交付删减；两者必须同时进入 `daily-trends.json` 与 `art-style-lottery-entry.json`。

## 4. Deliver：添加之后，必须删除

交付前执行 deletion audit。逐项删除以下没有独立价值的内容：

- 不能解释趋势的装饰、随机标签、假数据、无来源文字和 AI 味纹理。
- 不必要的渐变、发光、容器、边框和重复说明。
- 与主体争夺第一阅读层的细节，或重复表达同一判断的标题。
- 任何遮挡中文标题、核心判断、主体关系或证据入口的元素。

最后逐图/逐帧检查：16:9 比例、文字是否完整、是否折叠或溢出、主体与趋势是否有语义关系、三图构图是否真正不同、截图是否与最终文档一致。失败不能静默覆盖；必须保存 finding、回流阶段和 fix，并将结果写入案例记忆。

## Persistent Artifacts

每次视觉任务应尽量留下以下可回看的结构化字段：

| Artifact | Minimum content |
| --- | --- |
| `design_process` | framework、seed fingerprint、四阶段要求、独立 critic 协议和删除审计规则 |
| `direction_candidates` | 至少三个方向、差异维度、主观反应和选择理由 |
| `prompt_trace` | 场景、主体、关键细节、约束、输出意图；艺术家姓名不作为捷径 |
| `critic_reviews` | 截图输入、0-10 分、主要 finding、迭代次数和收敛判断 |
| `deletion_audit` | 删除了什么、为什么删、保留元素如何服务趋势 |
| `qa` | 可读性、重叠、比例、主题关系、surface/mechanism fidelity 和回流动作 |

结构化记录允许长程工作在上下文之外继续：下一轮读取契约和案例，不重新猜测过去做过什么，也不把原始研究和失败 prompt 塞回同一个上下文窗口。

## Safety and Credentials

- 生成图像时不把 API key 写入 prompt、仓库、HTML 或截图；凭据只放在本机环境或受控配置中。
- 参考图用于 moodboard 和质量基线，不复制具体作品、版式、可识别人物、logo 或受保护的文字内容。
- 文化视觉系统必须保留来源、时期、地域和机制证据；神圣符号、文物和传统不能被当作无语境装饰。
